@model FoodiesCore.ViewModels.HomePageViewModel

@{
    ViewBag.Title = "Home";
}

<h1>Welcome!</h1>

<div>
    <table>
        @foreach (var restaurant in Model.Restaurants)
        {
            <tr>
                <td>
                    <a asp-controller="Home" asp-action="Details" asp-route-id="@restaurant.Id">Detail</a>
                </td>
                <td>@restaurant.Name</td>
            </tr>
        }
    </table>
</div>

<div>
    <p>
        <a asp-controller="Home" asp-action="Create">Create a Restaurant</a>
    </p>
</div>

@section footer {
    <h3>@Model.CurrentMessage</h3>
}

