(function () {
    "use strict";

    // If the angular module needs any dependencies then they'll
    // be placed in the brackets.
    var app = angular.module("productManagement",
                            ["common.services"]);

}());